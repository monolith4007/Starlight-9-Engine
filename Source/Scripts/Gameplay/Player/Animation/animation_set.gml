/// animation_set(index, sprite, start, end, speed, loop_frame, loop_count, next, next_frame)
// Creates and sets a new animation for the player.
if (animation_index == argument0)
{
    animation_sprite     = argument1;
    animation_start      = argument2;
    animation_end        = argument3;
    animation_speed      = argument4;
    animation_loop       = argument5;
    animation_loop_count = argument6;
    animation_next       = argument7;
    animation_next_frame = argument8;
}
